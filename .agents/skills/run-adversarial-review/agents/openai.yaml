interface:
  display_name: "敵対的レビュー検証"
  short_description: "独立した反証によりレビューの見落としと根拠を検証します"
  default_prompt: "Use $run-adversarial-review to independently challenge this change and report evidence-backed findings."
policy:
  allow_implicit_invocation: false
